.DS_Store
.venv/
.pytest_cache/
__pycache__/
*.pyc

.env
.env.local

raw_data/
data/
.env
.env.*
!.env.example
*.db
*.sqlite
*.sqlite3

.coverage
htmlcov/

apps_sdk/web/node_modules/
apps_sdk/web/dist/
raw_data/

# Enrichment-pipeline generated artefacts (caches, run reports, disagreement dumps).
# Source code stays tracked; only the data it produces is gitignored.
enrichment/data/

# Private research notes / ongoing project context. Reports + plots only for
# the author's own reference; never pushed.
_context/

# Colleague-handoff subset exports (regeneratable from DB; large).
enrichment/exports/

# Sample data subset exports (from origin/main).
sample_data_enriched/

# Teammate-built dataset bundle (listings.db.gz + embeddings + landmarks;
# ~100 MB; installed into data/ by scripts/install_dataset.py).
datathon2026_dataset/

# Claude Code local settings + scheduler state (machine-local).
.claude/

report.md
