name: datathon2026
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -e .
      - pytest>=8.3.0
      # enrichment-pipeline extras (not required for the core API harness):
      # - reverse_geocoder: pass 1a offline geocoder (~2 MB GeoNames KDTree)
      # - pyyaml: reads enrichment/patterns/*.yaml for the regex pass
      # - openai: pass 2 GPT-5.4-mini structured extraction
      - reverse_geocoder>=1.5.1
      - pyyaml>=6.0
      - openai>=2.29.0
